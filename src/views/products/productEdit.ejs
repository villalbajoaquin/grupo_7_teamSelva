<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/products/productCreate.css">

    <script src="https://kit.fontawesome.com/d160ea4c64.js" crossorigin="anonymous"></script>
    <title>Editar producto</title>
</head>
<body>
    <header>
        <%- include('../partials/topheader') %>
    </header>
    <main>
        <div class="new-event">

            <div>
                <h2>Editar evento</h2>
            </div>

            <div class="container">
                <div class="standar-img"> <img src="/<%=shows.imgsrc%>" > </div>
                <form action="/product/product_edit/<%=shows.id%>?_method=PUT" method="POST">
                    <section class="info-show">
                        
                        <div>
                            <input type="text" value="<%=shows.name%>" name="show-name" placeholder="Nombre del show" required>
                        </div>
                      
                        <div>
                            <input type="datetime-local" value="<%=shows.date%>" name="show-date" placeholder="Fecha del show" required>
                        </div>

                        <div>
                            <input type="time" value="<%=shows.time%>" name="show-time" placeholder="Hora del show" required>
                        </div>

                        <div>
                            <input type="number" value="<%=shows.tickets%>" name="show-n-tickets" placeholder="Cantidad de tickets" required>
                        </div>

                        <div>
                            <input type="text" value="<%=shows.price%>" name="show-price" placeholder="Precio del show" required>
                        </div>

                        <div>
                            <input type="file" value="<%=shows.imgsrc%>" name="show-file" placeholder="Imagen del show" required>
                        </div>

                        <div class="button-send">
                            <button  href="/" type="submit">Enviar</button>
                        </div>    

                        <div class="button-reset">
                            <button type="reset">Borrar campos</button>
                        </div>
                    </section>

                </form>

            </div>

        </div>

    </main>


    
    <footer>
        <%- include('../partials/bottomfooter') %>
    </footer>
</body>
</html>